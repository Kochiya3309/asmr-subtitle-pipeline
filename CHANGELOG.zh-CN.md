# 更新日志

[English](CHANGELOG.md) | 简体中文

本文记录面向用户和架构层面的变化。只有本地 Git 历史能够提供明确版本边界时，才标注日期和提交链接。

仓库历史从 V3.3 源码快照开始。V3.0–V3.2 没有各自独立的提交或 tag；下列内容根据首次提交中保留的历史版本说明重建。

## [4.0.1] - 2026-09-08

### 新增

- 英文新手指南和英文开发者指南，并在每组中英文文档之间提供直接语言切换链接。
- 在中英文 README 顶部加入醒目的版本徽章，按语义化版本显示最新 Git tag，并链接至 GitHub Releases。

### 变更

- 英文 README 改为链接英文新手与开发者指南；中文 README 继续链接对应中文版。

## [4.0.0] - 2026-09-02

提交：[`379ff1f`](https://github.com/Kochiya3309/asmr-subtitle-pipeline/commit/379ff1f)

### 新增

- 结构化双 ASR 证据，保留模型原生指标、来源、对齐窗口、决策、救援计划和审计 manifest。
- 对缺少独立支持的固定重复幻觉模板进行高精度隔离，同时保留原始候选供复核。
- 有边界的救援转写：先由 large-v3 对可疑窗口进行无 VAD 扫描，只有 V3 检出可能语音时才运行 Turbo。
- 可选确定性融合时间轴：LLM 只选择文本和证据 ID，本地代码负责顺序和时间戳。
- 异常长字幕定时分析，支持正式应用的 `apply` 和不修改字幕的 `shadow` 模式。
- 可选 `127.0.0.1` 浏览器人工复核，支持双语上下文、日文/罗马音差异高亮、手动播放、时间编辑、字幕拆分与合并，以及折叠固定幻觉候选。
- 无损台本读取，支持 UTF-8/BOM、CP932/Shift-JIS 和经校验的混合编码行。
- 台本单元分类与确定性对齐：只把 dialogue 对齐到 ASR 时间窗，同时保留动作/心理说明、标题、装饰分隔符和 unknown 的原始跨度。
- 为音频预处理、ASR、融合、日文审校、翻译、人工复核和终审加入 SHA-256 输入绑定 manifest 与分阶段/原子写入。
- 统一音频收集与输出路径处理，并保留重复输入基名拒绝机制。
- 通过 `pykakasi` 提供人工复核罗马音提示；罗马音不参与 ASR 决策。

### 变更

- 台本模式现在也会运行 large-v3 和 large-v3-turbo。正式台本融合使用 large-v3 时间轴与台本文本；Turbo 保留为独立证据，并支持 mismatch 回退。
- 台本映射确认改为绑定音频和源台本内容，而不再只依赖修改时间。
- 非交互台本映射默认 fail-closed，只有明确设置 `SCRIPT_AUTO_VERIFY=1` 才能自动确认。
- 人工复核成为翻译与终审之间的可选门控。审核输出缺失或过期时，流水线暂停或阻止下游，不再静默绕过。
- 长字幕拆分与定时修改必须先通过本地校验，才能替换规范 SRT。
- 阶段复用由输入、输出、配置、生成参数和上游指纹共同决定，不再只判断文件是否存在。
- 支持的运行开关改由环境配置控制；`.env.example` 逐项记录模板中面向用户的设置。

### 修复

- 防止过期或仅写入一部分的字幕被当作完整缓存。
- 防止缺少有效证据的 LLM 文本在确定性模式下生成任意时间戳。
- 减少相同 ASR 候选、无害边界重叠和已知固定幻觉造成的人工审核噪声。
- 为旧人工复核提交增加迁移和指纹验证。
- 来源无法验证时保留旧产物，避免静默覆盖。

## [3.6.1] - 2026-08-19

提交：[`1a976e8`](https://github.com/Kochiya3309/asmr-subtitle-pipeline/commit/1a976e8)

### 修复

- 修复 V3.6 把 Python 命令、参数和 `pause` 合并到同一行后导致的四个 `start_*.bat` 启动失败。
- 将参数与脚本路径分开、恢复独立 `pause`、使用 CRLF 换行，并保持适合 Windows `cmd` 的 ASCII 内容。

## [3.6.0] - 2026-08-18

提交：[`0a53f93`](https://github.com/Kochiya3309/asmr-subtitle-pipeline/commit/0a53f93)

### 新增

- 通过 `OPENAI_API_KEY`、`OPENAI_BASE_URL`、`OPENAI_MODEL`、`OPENAI_ENABLE_THINKING` 和 `OPENAI_MAX_TOKENS` 配置 OpenAI 兼容 LLM。
- 新增与服务商无关的 `get_llm_client`，同时保留历史 DeepSeek 命名别名。
- 服务商拒绝最大输出 token 配置时，提供针对性排错提示。

### 变更

- DeepSeek 保持默认接口，同时允许不修改源码切换其他 OpenAI 兼容服务。
- 非标准 `thinking` 请求参数改为显式启用，避免标准服务商拒绝未知参数。
- LLM 配置在进程内缓存，避免同一次运行中发生配置漂移。
- 用量报告不再估算特定服务商费用，只保留调用和 token 统计。
- 不再向子进程注入空的可选服务商配置。
- 未设置 `OPENAI_API_KEY` 时继续兼容旧 `DEEPSEEK_API_KEY`。

## [3.5.0] - 2026-08-14

提交：[`3f923bb`](https://github.com/Kochiya3309/asmr-subtitle-pipeline/commit/3f923bb)

### 新增

- 直连 Tavily 和 Exa；两者同时配置时并行请求并按 URL 去重。
- 自动检测计算设备，并在 CUDA 不可用时回退 CPU。
- 不支持 `int8_float16` 的显卡自动回退 float16。
- 检测重复音频基名，避免不同扩展名的文件互相覆盖输出。
- 新增 `sitecustomize.py`，防止启动期间意外收到第一次 Ctrl+C 信号后直接崩溃。
- Windows 启动器加入 Hugging Face 镜像与项目路径设置。

### 变更

- 移除原智谱搜索及其密钥，搜索改为直接调用 Tavily/Exa。
- 将 faster-whisper 最低版本提高到 `1.2.1`，以支持 hotwords。
- 台本 mismatch 的 Turbo 补跑移到主线程串行阶段，避免 GPU 推理与并行 LLM 融合争用。
- 输入输出目录转为绝对路径，支持从项目外目录启动。
- 长请求的 token 估算改得更保守。
- 非交互台本匹配不再阻塞于 `input()`；该版本会自动接受当时生成的映射。V4.0.0 已把它改为通过 `SCRIPT_AUTO_VERIFY` 显式控制的 fail-closed 行为。

### 修复

- 修复空响应重试时工具调用历史不合法而产生 HTTP 400 的问题。
- `低信頼度`、`認識不良` 等低置信标记不再被误判为致命 API 残留。
- 同一批次中存在空译文时，其余有效翻译不再整批丢失。
- 重命名目标已存在时先备份，不再静默覆盖。
- 总台本拆分全部失败时能正确恢复，不再残留无效状态。
- 全篇审校批次过小时不再崩溃或静默跳过。
- 人工改坏的字幕块改为警告并跳过，不再使整个任务崩溃。
- GPU 检测改用 CTranslate2，移除对未声明 PyTorch 依赖的错误判断。

## [3.4.1] - 2026-07-15

提交：[`9dc0419`](https://github.com/Kochiya3309/asmr-subtitle-pipeline/commit/9dc0419)

### 安全

- 从 `run_all.py` 移除硬编码的 DeepSeek 和智谱 API Key，改为读取环境变量。
- 新增项目 `.env` 加载器并使用 `os.environ.setdefault`，使已有进程环境变量优先。
- 新增 `.env.example` 并忽略 `.env`，防止真实密钥进入 Git。

### 打包

- 加入 V3.4 音频预处理需要的 librosa、noisereduce、SciPy 和 soundfile。

## [3.4.0]

V3.4 功能实现与 V3.4.1 密钥加固一起进入提交 `9dc0419`；Git 历史中不存在独立的 V3.4 tag 或提交日期。

### 新增

- 可选 ASR 前处理：16 kHz 单声道读取、四阶 80 Hz Butterworth 高通滤波、保守稳态降噪、RMS 归一化至 -20 dBFS、峰值限制 0.95，以及 PCM-16 WAV 输出。
- 缓存 `*_preprocessed.wav`，支持转写断点续跑。
- ASMR 场景 `initial_prompt` 引导，以及用于人物名和专有词的 faster-whisper hotwords。

### 变更

- 针对低音量 ASMR 调整 VAD：threshold `0.3 → 0.2`、最短语音 `100 ms → 50 ms`、语音边缘填充 `600 ms → 800 ms`。
- 音频预处理依赖改为惰性加载，关闭预处理时不承担其导入开销。

### 当时记录的限制

- 稳态降噪主要针对底噪、嘶声、雨声等稳定背景音，并未声称能解决音乐或其他非稳态干扰。

## [3.3.0] - 2026-07-14

仓库首次源码快照：[`1f90643`](https://github.com/Kochiya3309/asmr-subtitle-pipeline/commit/1f90643)

### 新增

- 本地规则无法匹配台本时，使用 LLM 根据文件名判断对应关系。
- 检测一份总台本被多个音频命中，并强制进入拆分流程。
- 支持 `1A`、`1B` 等数字加字母的 track 标识符。
- 台本、音频或其文件列表变化时，使旧映射失效。

### 变更

- 台本模式跳过 Turbo，使用 large-v3 时间轴和台本文本融合。这项历史优化已不再是当前行为。
- 台本严重 mismatch 时补跑 Turbo 并自动回退普通 large-v3 + Turbo 融合，不再终止流水线。
- `script_mismatch.json` 只把 mismatch 文件送入日文二审；已匹配台本的文件跳过 STEP2。
- 翻译阶段从 `*_ensemble.srt` 发现任务，存在 `*_reviewed.srt` 时优先读取，使已审校与未审校输入可以混合处理。

### 修复

- 移除中文终稿验证中误报率较高的日文假名检查。

## [3.2.0]

根据 V3.3 快照中的历史说明重建；不存在独立 V3.2 提交或 tag。

### 新增

- 由 `ENABLE_SCRIPT` 控制的可选无时间轴日文台本辅助。
- `match_scripts.py` 作为 STEP0，提供同名/模糊匹配、正则优先的总台本拆分、LLM 拆分兜底和交互式映射确认。
- 使用台本文本辅助 large-v3 时间轴融合。
- 结构化融合错误字段，用于检测台本与音频不对应。V3.2 会在 mismatch 时停止；V3.3 改为自动回退。

## [3.1.0]

根据 V3.3 快照中的历史说明重建；不存在独立 V3.1 提交或 tag。

### 变更

- 把 STEP1 拆为所有文件串行 GPU 转写，再对所有文件并行执行 LLM 融合。
- 移除音频文件之间的固定等待。
- 将融合解析器提升至模块级，并在创建融合 worker 前初始化共享 LLM 客户端。

## [3.0.0]

根据 V3.3 快照中的历史说明重建；不存在独立 V3.0 提交或 tag。

### 新增

- 为日文审校、翻译审校和终审加入跨文件、跨批次并行；STEP1 融合在 V3.1 才进入这套并行模式。
- 在 `run_all.py` 集中配置，并通过环境变量传递给子脚本。
- 所有脚本共享追加写入的 `output/pipeline.log`。
- 新增 Whisper 模型缓存，使每个已加载 ASR 模型可跨音频文件复用。
- `strip_japanese.py` 作为可选纯中文导出阶段。
- 通用 `rename_suffix.py` 替代多个独立重命名工具。

### 变更

- 优化翻译提示词，处理不完整语句、拟声词、双关和标点一致性。
- 把结构化 Function Calling 扩展到字幕融合，并统一审校/修正输出、兜底解析和重试逻辑。
- 分批局部日文审校和全篇上下文审校均改为并行处理。
- 终审上下文推断改为通用逻辑，不再假设特定作品设定。
- 只有启用联网搜索时，才在提示词中加入搜索说明。
- 翻译失败时返回空占位，不再把日文复制到中文栏，以便验证阶段显式发现问题。

### 修复

- 不再缓存搜索错误，并修正未配置服务商密钥时的搜索缓存 miss 统计。

## 仅文档提交

- [`e3ea919`](https://github.com/Kochiya3309/asmr-subtitle-pipeline/commit/e3ea919)（2026-07-15）：在致谢中补充 SciPy 和 soundfile。
- [`d12acef`](https://github.com/Kochiya3309/asmr-subtitle-pipeline/commit/d12acef)（2026-08-27）：把原单语使用说明替换为英文 README 和简体中文 README，并调整忽略规则。
