# ASMR 字幕自动生成流水线 — 开发者文档（更新 V3.5.0）

## 项目概述

本项目的目标是从日语 ASMR 音频文件全自动生成中日双语字幕。技术路线分为五个阶段：先用 Whisper 双模型进行语音转写并融合为最优日语初稿，再用 DeepSeek-V4-Pro 对日语原文进行逐批审校加全篇上下文复查，然后将审校后的日语翻译为简体中文并逐段比对修正，接着将全篇字幕打包发送给 AI 进行宏观加微观终审（含上下文推断能力），最后通过纯规则脚本自动扫描残留的格式错误、API 拒绝消息、省略标记和审校注释残留。整个过程由六个 Python 脚本和一个入口调度器串联执行。所有中间产物保留为独立的 SRT 文件，方便在任意环节进行人工抽查或断点续跑。

项目的核心挑战在于四个方面。其一是 ASMR 音频的特殊性——耳语、吐息、唇齿音、环境触发音会显著降低 Whisper 的识别准确率，导致同音异义词误判、碎片化断句和重复输出。其二是大语言模型输出的不可靠性——DeepSeek-V4-Pro 在开启思考模式后偶尔会输出详细的分析报告而非简洁的修正结果，如果采用传统的纯文本分隔符解析方案，这种格式偏离会导致数据丢失。其三是 LLM 自身的安全机制可能在处理成人向内容时触发拒绝响应，将类似"可协助的内容仅限于安全与适当的部分"的错误消息写入字幕，这类 artifact 在纯 AI 驱动的审校环节中极难被自动发现。其四是 Whisper 同音词误判的隐蔽性——读音相同但汉字选错的词汇在局部上下文中完全合理，只有结合整部作品的世界观和术语分布才能察觉。本项目通过双模型融合、逐批审校加全篇上下文复查、Function Calling 结构化输出、三层兜底机制、终审上下文推断能力和末端的自动化规则验证来解决这四个问题。各版本的具体架构变化记录见文末「版本历史」章节。

---

## 系统运行方式与脚本间关系

整个流水线由七个 Python 文件、一个入口调度器和若干辅助脚本组成。

common.py 是共享函数库，所有其他脚本都从中导入统一的 DeepSeek 调用函数、Tavily/Exa 联网搜索函数、SRT 解析工具和全局 Token 统计模块。V3.0 中 call_deepseek 作为统一 API 调用入口，通过 output_tool 和 output_tool_parser 两个参数支持任意结构化输出工具（OUTPUT_TOOL、CORRECTION_TOOL、FUSION_TOOL），不再需要为每种工具编写独立的调用函数。三条 API 调用路径（纯流式、非流式结构化输出、搜索加结构化输出）合并为两条（_call_streaming 和 _call_non_streaming），自动重试逻辑内嵌在 _call_non_streaming 中。三层兜底解析逻辑收敛为 parse_review_result 函数，review_japanese.py 和 translate.py 共享同一套代码。全局搜索缓存、Token 统计、API 客户端缓存（模块级单例）也在此维护。V3.0 新增文件日志功能：模块加载时检查 LOG_FILE 环境变量，如果存在则 monkey-patch builtins.print，使所有 print 调用同时写入终端和日志文件。

ensemble_transcribe.py 负责第一阶段——调用本地 faster-whisper 库，用 large-v3 和 large-v3-turbo 两个模型分别转写同一份音频。V3.0 新增 Whisper 模型缓存（_model_cache 字典），处理多个音频文件时每个模型只加载一次。V3.1 起脚本重构为两阶段分离架构：阶段1由 transcribe_one_audio 函数串行处理所有音频文件的双模型转写（GPU 独占，避免显存争用），Whisper 模型实例跨文件复用；阶段2由 fuse_one_audio 函数配合 ThreadPoolExecutor 并行调用 DeepSeek 对所有文件的两份 SRT 进行融合（并发数由 MAX_WORKERS 环境变量控制，默认 10）。两阶段分离后，转写阶段的 GPU 瓶颈仍串行，但融合阶段的网络等待被并发摊薄，多文件场景下 ensemble 总耗时缩短约 20%。_fusion_parser 从闭包提升为模块级函数，避免每次调用重定义。阶段2开始前调用 get_deepseek_client() 预初始化客户端单例，消除多线程首次调用时的 race。去掉文件间的 time.sleep(5) 固定等待。融合阶段改用 submit_fusion 工具进行 Function Calling 结构化输出，_fusion_parser 函数从工具参数中提取 subtitles 数组并组装为 SRT 格式字符串。纯文本回退（extract_srt_from_response 加 _fix_srt_indexing）仍保留作为兜底。融合提示词包含关键约束——明确要求 AI 不得省略或丢弃仅在一个模型中存在的内容，禁止使用省略符号。V3.2 起脚本启动时调用 check_script_mapping 检查台本映射文件与验证标记，缺失或未验证则退出；台本文件 mtime 与验证标记 mtime 比较，台本更新则标记失效。fuse_one_audio 函数签名增加 script_text 参数，非空时在 user_input 中插入"公式台本"段落并附加台本使用约束。_fusion_parser 优先检测 error 字段，返回 __SCRIPT_MISMATCH__:<原因> 特殊标记。V3.3 起重构为双模式：台本模式（script_text 非空）跳过 Turbo 转写（transcribe_one_audio 新增 has_script 参数），融合采用简化的 V3+台本 prompt（不再对比 V3 与 Turbo）；无台本模式保留原 V3+Turbo prompt 与逻辑完全不变。mismatch 处理从 sys.exit(1) 改为自动回退：fuse_one_audio 新增 _is_retry 参数，检测到 mismatch 时将 base 加入模块级 _mismatch_files 集合，递归调用自身以无台本模式重新融合（按需补跑 Turbo）。main 函数结束后将 _mismatch_files 写入 output/script_mismatch.json，不再终止流水线。无台本模式下行为与 V3.2 完全一致。

match_scripts.py 是 V3.2 新增的台本预处理脚本，作为 STEP0 在 ensemble 之前运行。负责扫描 audio/ 和 scripts/ 目录，对每个音频尝试同名匹配（scripts/<audio_base>.txt）、模糊匹配（extract_track_id 提取开头标识符支持 1A/1B/01/track01 等形式，extract_keywords 去掉 track 编号和常见后缀后在台本内容中搜索关键词），对未匹配音频从合并台本中拆分。V3.3 新增合并台本检测：阶段1结束后扫描模糊匹配结果，若某台本被 ≥2 个音频命中（排除同名匹配），自动将这些条目转为 pending_split 状态，触发拆分流程。V3.3 新增阶段1.5 LLM 文件名匹配：正则匹配失败的未匹配音频与候选台本文件名发送给 LLM（MATCH_TOOL 工具，Function Calling 输出 [{audio_name, action, script_file}, ...]，action 为 match/split/none），LLM 根据文件名判定对应关系。拆分优先用正则识别标题分隔符（Track N、第N話、【〇〇】、N.、■ 等多种模式），正则失败时转 LLM 拆分（日文 system_prompt 加 SPLIT_TOOL 工具）。拆分结果写入 output/scripts_split/<audio_name>.txt。合并台本无法拆分时根据 SCRIPT_FALLBACK_FULL 开关决定是否整本发送给每个未匹配音频。生成 output/script_mapping.json 映射文件后进入人工验证环节。V3.3 扩展幂等检查为四类失效条件：台本文件在验证后被修改、音频文件列表变化（新增或移除）、音频文件在验证后被修改、台本文件列表变化（出现新台本未被旧映射包含）。任一条件命中均打印具体原因并重新匹配。台本文件修改后标记自动失效（mtime 比较）。FORCE_RESPLIT 开关可强制清理旧拆分文件重新处理。SPLIT_TOOL、MATCH_TOOL 及对应 _split_parser、_match_parser 定义在脚本内部，仅此脚本使用。

review_japanese.py 负责第二阶段，分为两个子阶段。阶段一是逐批审校：读取融合后的日语字幕，按每 30 条一组发送给 DeepSeek，相邻组之间有 5 条重叠以消除上下文断裂。系统提示词中包含了成人向内容的类型提示以及低品质音频误识别的检测指引。无法识别的内容统一标记为「〔認識不良〕」。阶段二是全篇上下文审校：在所有批次处理完成后，将全篇日语字幕打包发送给 AI，利用完整的作品世界观和术语一致性来修正逐批审校无法发现的同音词误判。V3.0 起两个阶段均支持跨文件并行处理。搜索相关提示词根据 ENABLE_SEARCH 开关动态拼接。两个阶段均通过 submit_review 工具提交结构化结果，享受相同的三层兜底保护。V3.3 起启动时检测 output/script_mismatch.json，若存在且非空则仅审校列表中的文件（mismatch 回退产物），文件不存在时行为与 V3.2 完全一致。STEP2 整体是否运行由 run_all.py 根据 ENABLE_SCRIPT 与 mismatch 列表动态决定。

translate.py 负责第三阶段，分为两个子阶段。翻译阶段按每 10 条一组将日语批量翻译为简体中文，纯流式调用，不开启搜索以节省时间和费用。V3.0 优化了翻译提示词，新增拟声词处理、不完整句子翻译、双关语处理、括号格式统一等规则。审校阶段按每 20 条一组将原文和初译并列发送给 AI 进行比对修正，使用 submit_review 结构化输出。低质量原文的处理方式改为始终输出中文译文并标注「（低信頼度）」，不再在译文栏保留日语原文。翻译失败时返回空占位字符串而非日语原文，确保译文栏始终是中文或空。V3.0 起两个阶段均支持跨文件并行。搜索相关提示词根据 ENABLE_SEARCH_IN_REVIEW 开关动态拼接。V3.3 起输入逻辑重构：以 *_ensemble.srt 为基础扫描（PATTERN 常量移除），对每个文件优先使用 *_reviewed.srt，不存在则回退到 *_ensemble.srt；base 提取兼容 _reviewed.srt 与 _ensemble.srt 两种后缀。该改动使台本模式跳过 STEP2 时 STEP3 仍可正常运行，混合场景（mismatch 文件经审校、其余未审校）亦兼容。

review_final.py 负责第四阶段——将全篇字幕（日中对）打包为一个整体发送给 DeepSeek，让 AI 通读全文后同时从宏观和微观两个角度进行终审。V3.0 起终审支持跨文件并行。系统提示词中的上下文推断能力经过泛化优化：要求 AI 先判断作品题材和舞台设定，再以此为基准检查不合理词汇；误听案例保留但标注为"仅供理解，不可套用"；新增通用的误听模式分类（同音异义词、场景矛盾词、拟声词混淆、专有名词误识别），适用于任何题材。V3.0 起不再使用独立的 _final_review_call 函数，改用 common.call_deepseek 传入 CORRECTION_TOOL 和 _corrections_parser。搜索相关提示词根据 ENABLE_SEARCH 开关动态拼接。

validate_final.py 负责第五阶段——纯规则扫描，不调用任何 AI，几秒跑完。检查所有 _final.srt 文件中是否存在空译文、裸编号残留、纯标点、API 安全拒绝模板（含日文版）、省略和跳过标记（中略、省略、認識不良、低信頼度等）、日语原文为空或过短、以及日语原文栏中残留的 AI 审校注释。问题分为严重和警告两个等级。发现严重问题时返回非零退出码，run_all.py 会终止流水线。V3.0 起该脚本 import common 以触发日志初始化。V3.3 起移除中文译文中的日文假名残留检查（JAPANESE_KANA 正则常量及对应检测逻辑一并移除），原因为误报率高且实际意义有限。

strip_japanese.py 是可选的后处理脚本，从 _final.srt 中去除日文行，仅保留中文翻译，输出 _cn_only.srt。V3.0 起集成为 run_all.py 的 STEP6_STRIP 步骤。支持多行中文译文（取第 3 行起所有内容）。修复了条目数统计 bug（空文件时返回 0 而非 1）。

rename_suffix.py 是通用的重命名工具，通过命令行参数指定后缀（如 _cn_only 或 _final），将对应后缀的 SRT 文件重命名为 .srt。合并了 V2.0 中的 rename_cn_only.py 和 rename_final.py。

run_all.py 是入口调度器，负责设定环境变量（API Key、目录路径、功能开关、批次参数、并发数）、按序调用六个处理脚本（STEP1 到 STEP6），并支持通过布尔开关跳过任意步骤。V3.4.1 起 API Key 不再硬编码于源码，改为从环境变量读取（优先系统环境变量，回退到项目根目录的 .env 文件，手动解析无 python-dotenv 依赖）。V3.0 新增 MAX_WORKERS、REVIEW_JP_BATCH_SIZE、REVIEW_JP_OVERLAP、REVIEW_JP_FULL_REVIEW、REVIEW_JP_FULL_REVIEW_BATCH、TRANSLATE_BATCH_SIZE、TRANSLATE_REVIEW_BATCH_SIZE 等参数，全部通过环境变量传递给子脚本。V3.0 新增 LOG_FILE 环境变量和文件日志功能。在 import common 之前设置 LOG_FILE 环境变量并加载 .env 文件，确保日志初始化与 Key 读取在所有脚本中生效。V3.5 起在 import common 前打印"正在加载依赖库"提示（依赖加载较慢时让用户知道程序未卡死）。非交互式终端检测（sys.stdin.isatty）避免在计划任务中阻塞。V3.3 起流程改为两阶段执行：先跑 STEP0+STEP1，再根据 output/script_mismatch.json 决定是否跑 STEP2，最后跑 STEP3-6。台本模式下 STEP2 默认跳过（台本即权威文本无需审校），仅当 mismatch 列表非空时触发（review_japanese.py 会自动仅审校 mismatch 文件）。无台本模式下 STEP2 始终运行，行为与 V3.2 完全一致。

sitecustomize.py 是启动防御脚本（V3.5 新增）。个别机器上存在未知程序向控制台进程组注入 Ctrl+C 信号，导致 Python 在启动 import 阶段抛出 KeyboardInterrupt 崩溃。该脚本通过 PYTHONPATH 在 Python 启动早期（site 模块初始化阶段，早于任何第三方库 import）自动加载，将 SIGINT 处理替换为防抖逻辑：第 1 次 Ctrl+C 仅打印提示并忽略，第 2 次才真正抛出 KeyboardInterrupt（保留人工强制中断能力）；非主线程或无信号支持的环境自动跳过。start.bat 等启动脚本已设置 PYTHONPATH 指向项目目录使其生效。

脚本之间的数据传递完全通过文件系统完成。每一步的输出都是命名规范的 SRT 文件，存放在统一的 output 目录下。文件命名以原始音频文件名为前缀，加上步骤标识后缀——_ensemble、_reviewed、_zh、_final、_cn_only。这种设计使得任何一步失败后都可以单独重新运行，而已完成的步骤会自动跳过。

---

## 核心设计决策

**Function Calling 替代纯文本解析。** 这是项目中最关键的架构决策。V3.0 将此决策扩展到融合阶段——新增 submit_fusion 工具，参数为 subtitles 数组（每个元素包含 timecode 和 text）。至此，所有需要结构化输出的环节（融合、逐批审校、全篇终审）均使用 Function Calling。V3.0 进一步将四条 API 调用路径（纯流式、结构化输出、搜索加结构化输出、终审独立函数）合并为两条（_call_streaming 和 _call_non_streaming），通过 output_tool 和 output_tool_parser 参数差异化。格式偏离的发生率从约百分之十到十五降到了接近零。

**三层兜底机制。** V3.0 将三层兜底解析逻辑从 review_japanese.py 和 translate.py 中收敛为 common.py 中的 parse_review_result 函数。两个脚本传入不同的 per_line_retry_fn 回调（各自的逐条重试 prompt），公共的拆分、校验、降级逻辑复用。自动重试逻辑（content 为空时 budget_tokens 降至 4096）内嵌在 _call_non_streaming 中，不再有四份副本。

**逐批审校加全篇上下文复查的双阶段架构。** 阶段一的逐批审校每批只有 30 条字幕的上下文，无法判断在整部作品中只出现一次的词是否选错了汉字。阶段二的全篇上下文审校解决了这个盲区。V3.0 起两个阶段均支持跨文件并行——所有文件的批次被收集到一个列表中同时发送。

**全阶段并行处理。** V3.0 的核心性能优化，V3.1 进一步扩展到 ensemble 融合阶段。DeepSeek API 调用阶段（ensemble 融合、日语审校、翻译、审校、终审）均改为跨文件加跨批次并行：用 ThreadPoolExecutor 收集所有文件的所有批次，同时发送 API 请求。MAX_WORKERS 默认为 10，DeepSeek API 并发限制为 500，余量充足。Whisper 转写阶段因 GPU 独占保持串行，但模型实例跨文件复用。V3.1 起 ensemble_transcribe.py 也改为两阶段分离——转写阶段串行（GPU 独占），融合阶段并行（网络 I/O），消除原"每文件转写+融合串行"模式下融合阶段的串行等待。并行环境下日志输出带文件名前缀（如 [文件名]）以区分不同文件。3 个 1 小时音频的总耗时从 V2.0 的约 82 分钟降至 V3.0 的约 52 分钟，V3.1 进一步降至约 44 分钟。

**双模型 Ensemble 与模型缓存。** V3.0 新增 _model_cache 字典，WhisperModel 实例跨文件复用。处理 N 个音频文件时，large-v3 和 large-v3-turbo 各只加载一次（共 2 次 GPU 到显存拷贝），而非 V2.0 的 2N 次。

**终审上下文推断能力的泛化。** V2.0 的四个误听案例全部来自同一部 SF 实验题材作品，在其他题材中可能误导 AI。V3.0 改为：要求 AI 先判断作品题材和舞台设定；误听案例保留但标注为"仅供理解，不可套用"；新增通用的误听模式分类（同音异义词、场景矛盾词、拟声词混淆、专有名词误识别），适用于任何题材。

**搜索提示词动态拼接。** V2.0 中多个提示词无条件提及 web_search，但 ENABLE_SEARCH 默认为 False。AI 看到提示词说可以搜索却找不到工具，可能产生困惑或空输出。V3.0 改为根据 ENABLE_SEARCH 开关动态拼接搜索相关指令。涉及 review_japanese.py 的逐批审校和全篇审校、translate.py 的审校阶段、review_final.py 的终审。

**文件日志。** V3.0 新增。通过 monkey-patch builtins.print 实现，所有 print 调用同时写入终端和 output/pipeline.log。日志文件以追加模式打开，每次运行写入带时间戳的分隔头。子脚本通过 import common 自动激活，无需额外配置。

**参数集中管理。** V3.0 将所有可调参数（批次大小、重叠条数、全篇审校开关、并发数等）集中到 run_all.py 顶部配置区，通过环境变量传递给子脚本。子脚本从环境变量读取配置，环境变量不存在时使用内置默认值。用户无需翻进子脚本修改参数。

**翻译失败处理。** V2.0 中翻译失败时返回日语原文作为译文，导致中文译文栏出现日语。V3.0 改为返回空占位字符串，翻译失败的条目在审校阶段会被 AI 重新翻译（因为初译为空）。如果审校也没翻出来，validate_final.py 会报"空译文"严重问题，提示人工补译。

**全局搜索缓存与错误处理。** V3.0 修复了搜索缓存的两个问题：错误结果不再被缓存（避免后续命中返回错误信息）；未配置 TAVILY_API_KEY / EXA_API_KEY 时不计入 miss 计数。

**台本辅助与人工验证。** V3.2 新增，V3.3 优化。发行商台本作为权威文本参考注入 ensemble 融合阶段，提升 Whisper 误听修正能力。台本处理流程：同名匹配 → track 标识符匹配（支持 1A/1B）→ 关键词匹配 → 合并台本检测（≥2 命中转 pending_split）→ LLM 文件名匹配（判定 match/split/none）→ 正则拆分 → LLM 拆分。结果必须经人工验证（JSON 映射加 .verified 标记文件双轨）方可进入 ensemble。V3.3 幂等检查扩展为四类失效条件：台本修改、音频列表变化、音频修改、台本列表变化。V3.3 台本模式效率优化：有台本时跳过 Turbo 转写（仅 V3），融合采用简化 prompt；mismatch 时自动回退到无台本模式（补跑 Turbo + V3+Turbo 融合），记录到 script_mismatch.json，不再 sys.exit 终止流水线。STEP2 日语二审在台本模式下默认跳过，仅当 mismatch 列表非空时触发且仅审校 mismatch 文件。台本与 Whisper 严重不对应时 AI 通过 FUSION_TOOL 的 error 字段报告（subtitles 与 error 二选一，required 改为空数组）。合并台本无法拆分时 SCRIPT_FALLBACK_FULL=True 兜底为整本发送给每个未匹配音频，超 token 限制则报错退出。台本使用约束：非台词说明不入字幕、台本文本优先、Whisper 时间轴优先、台本仅有发话也保留、部分不一致属于修正对象而非 error 报告对象。无台本模式下整条台本流程跳过，行为与 V3.2 完全一致。

---

## 常见故障排查

**终端出现"件数不一致"横幅。** 在当前版本中，此横幅出现后会自动进入防线二或防线三，数据不会真正丢失。如果伴随出现"content 为空，自动重试"，说明自动重试机制已被触发。如果重试成功，后续不会进入三层兜底。如果重试也失败，才会进入正则降级或逐条重试。如果几乎每批都出现此横幅且全部走了逐条重试，说明 Function Calling 完全没有生效，可能的原因包括工具定义被意外修改、API 服务不稳定、或 max_tokens 配额严重不足。

**终端输出交错混乱。** V3.0 并行处理时多个文件的批次同时输出，终端会交错。每行输出带文件名前缀（如 [文件名]）以区分。完整有序的输出在 pipeline.log 中。

**翻译结果出现重复或与原文对不上的情况。** 这通常是源头问题——Whisper 的原始转写已经存在严重碎片化或卡带式重复。检查 _v3.srt 和 _turbo.srt 的条目数是否合理，典型的一小时 ASMR 约两百到四百条。如果条目数超过一千，说明 Whisper 的参数需要调整——在 ensemble_transcribe.py 的 transcribe_with_model 函数中确认条件抑制参数（condition_on_previous_text=False、no_repeat_ngram_size=5、repetition_penalty=1.5、temperature=0.0）均已设置。

**某个步骤长时间无输出或 result 为 None。** 首先确认网络是否可达 DeepSeek API，其次检查 API Key 是否有效及余额是否充足。如果余额充足且网络正常，问题可能出在 max_tokens——如果 budget_tokens 设得太高，而输入文本又很长，模型可能在思考阶段消耗了大部分配额。此时 common.py 的自动重试机制会以极简思考模式重试一次，如果重试也失败才会返回 None。如果频繁出现此情况，考虑降低 budget_tokens 或增大 max_tokens。

**AI 在不开搜索时 content 为空。** 这是不开搜索时的一个特殊副作用。V4-Pro 的思维链遇到罕见词时会反复推敲猜测，导致思维链过度膨胀、配额耗光。common.py 的自动重试机制专门针对这一场景设计——重试时 budget_tokens 降至 4096，迫使 AI 精简思考，通常能成功。如果仍频繁出现，建议开启搜索功能或降低 BATCH_SIZE。V3.0 的搜索提示词动态拼接也减少了此类问题——搜索关闭时 AI 不会看到任何关于 web_search 的提及，不会尝试搜索。

**validate_final.py 报告发现 API 残留。** 说明 DeepSeek 在处理某条字幕时触发了安全拒绝机制。处理方法：打开对应的 _final.srt，找到被标记的条目，手动补译。如果多部作品频繁出现此类问题，说明当前使用的 API 对成人向内容的容忍度不足以支撑使用场景，可能需要更换 API 提供商或接受手动补译。

**validate_final.py 报告发现日语注释残留。** 说明日语原文栏中包含了 AI 审校阶段留下的思考注释（如 ※文脈によっては…）。这通常发生在日语审校中 AI 输出了分析报告而解析失败时。需要追溯到对应的 _reviewed.srt 和 _ensemble.srt，找到干净版本的日语原文替换。

**validate_final.py 报告发现内容省略。** 说明字幕中存在（中略）、（省略）等硬省略标记，属严重问题。这可能是 ensemble 融合阶段或翻译阶段 AI 在不确定时使用了省略标记。如果是 ensemble 阶段引入的，说明融合提示词中禁止省略的约束未生效——检查 ensemble_transcribe.py 的 system_prompt。如果是翻译阶段引入的，检查 translate.py 审校阶段的提示词。注意「低信頼度」「認識不良」等低置信度标记（V3.5 起）属警告级别，是上游 prompt 明确要求生成的设计内产物，不阻断流水线，仅提示人工复查。

**翻译失败导致译文栏出现日语。** V2.0 中翻译失败时返回日语原文作为译文。V3.0 已修复——翻译失败时返回空占位字符串，validate_final.py 会检测到空译文并报告。如果仍出现此问题，检查 translate.py 的 _translate_batch 函数是否返回 [""] * len(batch_texts) 而非 batch_texts。

---

## 调试技巧

查看完整的 AI 思维链是理解模型行为最直接的方式。V3.0 默认关闭思维链显示（show_reasoning=False），API 调用仍正常统计 token 但不打印到终端。如需观察思维链，在调用 call_deepseek 时传入 show_reasoning=True。

单独调试某个步骤最快的方式是在 run_all.py 中把其他步骤的开关全部设为 False，只保留需要调试的那一步。由于每一步的输出文件独立存在，跑单步不会影响其他步骤的产物。也可以直接手动调用单个脚本，设置好环境变量后在 CMD 中直接运行。

每一个中间产物都是独立的 SRT 文件，可以直接用文本编辑器打开人工审查。追踪一个翻译错误是从哪个环节引入的，方法是沿着文件链逐级对比——打开 _final.srt 找到问题条目，再打开 _zh.srt 确认是否已在审校阶段出现问题，再打开 _reviewed.srt 确认日语原文是否正确，最后打开 _ensemble.srt、_v3.srt 和 _turbo.srt 追溯到 Whisper 的原始输出。

每个脚本跑完都会自动打印 Token 报告，包含调用的 API 次数、输入 token 总量、缓存命中数和命中率、输出 token 总量和精确费用。所有输出同时写入 output/pipeline.log。如果需要在不跑完整脚本的情况下查看统计，可以从 common 导入 get_usage_report 函数直接调用。如果需要清空搜索缓存重新测试搜索功能，调用 common 中的 clear_search_cache 函数即可。

强制重新处理某个文件的方法很简单——直接删除该文件对应的输出文件，重新运行脚本。对于 ensemble_transcribe.py，中间产物（_v3.srt 和 _turbo.srt）也可以单独删除以强制重新转写。

V3.0 的并行处理使得断点续跑更加灵活：如果某个文件的某个批次失败了，其他文件的批次不受影响。失败的批次会用原文填充，不会阻塞整个流水线。重新运行时已成功的文件会跳过，只有未完成的文件会重新处理。V3.1 起 ensemble 阶段也支持这种灵活续跑——_v3.srt / _turbo.srt 存在则跳过转写，_ensemble.srt 存在则跳过融合，两个阶段可独立续跑。

---

## 关键参数说明

所有参数在 V3.0 中已集中到 run_all.py 顶部配置区，通过环境变量传递给子脚本。

MAX_WORKERS（默认为 10）控制 DeepSeek API 的并发数。DeepSeek API 并发限制为 500，可以放心调大。数值越大并行度越高、耗时越短，但对本地网络带宽和 CPU 调度压力也越大。

REVIEW_JP_BATCH_SIZE（默认为 30）控制日语逐批审校每批条数。增大 BATCH_SIZE 会减少 API 调用次数，但会增加单批次的复杂度。减小 BATCH_SIZE 会提高格式稳定性和审校精度，代价是 API 调用次数增加。

REVIEW_JP_OVERLAP（默认为 5）控制相邻批次之间的重叠条数，消除批次边界的上下文断裂。并行环境下重叠部分的结果为先到先得（is None 检查），不影响数据完整性。

REVIEW_JP_FULL_REVIEW（默认为 True）控制是否在逐批审校后执行阶段二的全篇审校。REVIEW_JP_FULL_REVIEW_BATCH（默认为 200）控制全篇审校时单批发送的最大条目数。如果总条目数超过此值，会分批处理，带 10 条重叠。

TRANSLATE_BATCH_SIZE（默认为 10）控制翻译阶段每批条数。TRANSLATE_REVIEW_BATCH_SIZE（默认为 20）控制翻译审校阶段每批条数。

max_tokens（默认为 131072）控制 DeepSeek 单次请求的总输出配额，包含思维链 token 和最终回答 token。budget_tokens（默认为 65536）是为思维链预留的专项配额。自动重试时 budget_tokens 会降至 4096。MAX_SEARCH_ROUNDS（默认为 3）控制单次对话中 AI 最多能进行多少轮搜索。

beam_size（默认为 5）控制 Whisper 转写的搜索宽度。temperature（默认为 0.0）使用贪婪解码，消除随机抖动导致的碎片化重复。condition_on_previous_text（默认为 False）是抑制 Whisper 重复行为的关键参数。no_repeat_ngram_size（默认为 5）和 repetition_penalty（默认为 1.5）分别从序列层面和概率层面对重复施加约束。

ENABLE_SCRIPT（默认为 False）是 V3.2 新增的台本功能总开关。True 时 ensemble_transcribe.py 启动会检查台本映射文件与验证标记，缺失或未验证则退出；False 时整条台本流程跳过，行为与 V3.1 完全一致。STEP0_MATCH_SCRIPTS（默认为 False）控制是否在 ensemble 之前运行 match_scripts.py。SCRIPT_DIR（默认为 ./scripts）指定台本文件所在目录。SCRIPT_FALLBACK_FULL（默认为 True）控制合并台本无法拆分时是否整本发送给每个未匹配音频。SCRIPT_FORCE_RESPLIT（默认为 False）控制是否强制清理旧拆分文件重新处理（忽略缓存）。

---

## 代码组织

common.py 是整个项目的基石，包含以下模块：文件日志初始化（monkey-patch builtins.print）、全局搜索缓存字典及管理函数、全局 Token 统计字典及精确计费报告函数、API 客户端获取函数（模块级缓存）、四个工具定义（SEARCH_TOOL、OUTPUT_TOOL 用于 submit_review、FUSION_TOOL 用于 submit_fusion 含 error 字段支持台本不匹配报错、CORRECTION_TOOL 定义在 review_final.py 中）、Tavily/Exa 联网搜索函数（V3.5 起替换智谱：get_search_keys、_http_post_json、_tavily_search、_exa_search、web_search，含标准化和 MD5 缓存逻辑、双提供商并行合并去重，错误结果不缓存）、降级提取函数 try_extract_lines_from_analysis、三层兜底解析函数 parse_review_result、统一入口 call_deepseek（根据 stream、output_tool、enable_search 参数分发到两条内部路径）、两条内部路径（_call_streaming 纯流式含 usage 统计、_call_non_streaming 结构化输出加搜索加自动重试）、SRT 工具函数（extract_srt_from_response、format_timestamp、parse_srt、parse_srt_full）、以及 V3.2 新增的多编码读取函数 read_text_file（按 utf-8-sig、utf-8、cp932、shift_jis、euc-jp 顺序尝试，替换字符占比 1% 阈值校验，用于读取发行商提供的 Shift-JIS 台本）。

ensemble_transcribe.py 包含 Whisper 模型缓存（get_whisper_model）、_detect_device 函数（V3.5 新增，通过 ctranslate2 的 get_cuda_device_count 检测 CUDA 设备：有 CUDA 设备用 cuda/int8_float16，无则回退 cpu/int8。注意本项目不依赖 torch，faster-whisper 的 GPU 加速由 ctranslate2 提供，用 torch 判断 CUDA 会误判）、get_whisper_model 的降级兜底（V3.5 新增：老 GPU 加载 int8_float16 失败时自动降级 float16 重试，其余错误照常抛出）、V3.4 新增的转写优化配置区（ENABLE_AUDIO_PREPROCESS、INITIAL_PROMPT、HOTWORDS 及四个 VAD 参数，均从环境变量读取）、preprocess_audio 函数（V3.4 新增，librosa 读取 + 80Hz 高通滤波 + noisereduce 稳态降噪（prop_decrease=0.7）+ RMS 归一化（峰值限幅 0.95）+ soundfile 输出，依赖惰性导入）、transcribe_with_model 函数（V3.4 起转写前调用预处理并支持缓存断点续跑，VAD 参数从环境变量读取，按需附加 initial_prompt/hotwords，调用 faster-whisper 并输出 SRT，含全套抗重复参数）、_fix_srt_indexing 函数（序号修复）、_fusion_parser 函数（模块级，优先检测 error 字段返回 __SCRIPT_MISMATCH__ 标记，否则从工具参数提取 subtitles 并组装为 SRT）、check_script_mapping 函数（V3.2 新增，启动时检查映射文件与验证标记，返回 {audio_base: script_text} 字典）、transcribe_one_audio 函数（阶段1：V3 加 Turbo 双模型转写，GPU 独占串行；V3.3 新增 has_script 参数，有台本时跳过 Turbo）、fuse_one_audio 函数（阶段2：双模式——台本模式用 V3 加台本简化 prompt，无台本模式用 V3 加 Turbo 原 prompt；V3.3 新增 _is_retry 参数支持 mismatch 自动回退；V3.5 起 Turbo 缺失时返回 pending_turbo 状态而非在线程池内补跑）、模块级 _mismatch_files 集合（V3.3 新增，线程安全记录 mismatch 文件名）与 _needs_turbo 集合（V3.5 新增，Turbo 补跑队列）、_check_duplicate_bases 函数（V3.5 新增，检测去掉扩展名后重名的音频）、collect_audio_files 函数、main 函数（编排：先串行调用 transcribe_one_audio 收集成功文件，再预初始化 DeepSeek client 后用 ThreadPoolExecutor 并行调用 fuse_one_audio，随后在阶段2.5 主线程串行补跑 Turbo 并重新融合 pending 文件；结束后将 _mismatch_files 写入 output/script_mismatch.json，不再 sys.exit）。

match_scripts.py 是 V3.2 新增脚本，V3.3 扩展。包含 SPLIT_TOOL 和 MATCH_TOOL 定义及对应 _split_parser、_match_parser 解析器（内部使用）、collect_audio_files 和 collect_script_files 函数、_check_duplicate_bases 函数（V3.5 新增，检测去掉扩展名后重名的音频）、extract_keywords 函数（从音频文件名提取关键词，去掉 track 编号和常见后缀）、extract_track_id 函数（V3.3 替代 extract_track_number，支持 1A/1B 等数字+字母组合，统一大写去前导零）、try_same_name_match 和 try_fuzzy_match 函数、split_by_regex 函数（正则识别 Track N、第N話、【〇〇】、N.、■ 等标题分隔符）、split_by_llm 函数（LLM 拆分，日文 system_prompt）、llm_match 函数（V3.3 新增，LLM 文件名匹配，返回 {audio_name: {action, script_path}} 字典）、main 函数（编排：阶段1本地匹配 → 阶段1补充合并台本检测 → 阶段1.5 LLM 文件名匹配 → 阶段2拆分 → 阶段3兜底处理 → 写映射 → 人工验证交互；V3.3 幂等检查扩展为四类失效条件；V3.5 起拆分全失败或结果缺失时整组兜底进 unmatched、非交互终端自动通过验证）。

review_japanese.py 包含两个阶段的审校逻辑，均支持跨文件并行。阶段一：main 函数收集所有文件的所有批次，用 ThreadPoolExecutor 并行调用 _process_batch。_process_batch 构建审校输入和含成人向语境提示的系统提示词，调用 call_deepseek 的结构化输出路径，然后进入 parse_review_result 三层兜底解析。阶段二：_full_context_review 函数在全篇审校开启时跨文件并行调用 _full_review_batch。搜索相关提示词根据 ENABLE_SEARCH 动态拼接。

translate.py 分为 _translate_batch（翻译阶段，纯流式，翻译失败返回空占位）和 _review_batch（审校阶段，结构化输出，低质量原文始终输出中文译文加低信頼度标注）两个函数。main 函数收集所有文件的所有翻译批次和审校批次，分别用 ThreadPoolExecutor 并行处理。搜索相关提示词根据 ENABLE_SEARCH_IN_REVIEW 动态拼接。

review_final.py 使用 CORRECTION_TOOL 和 _corrections_parser 通过 common.call_deepseek 提交修正清单。system_prompt 包含题材自适应引导、误听模式分类、四步推断流程、标志性异常模式识别、宏观检查和微观检查。搜索相关提示词根据 ENABLE_SEARCH 动态拼接。main 函数用 ThreadPoolExecutor 跨文件并行处理。

validate_final.py 是纯规则扫描脚本，import common 以触发日志初始化。定义了 API_ARTIFACT_PATTERNS 列表、BARE_INDEX 正则、BARE_PUNCT 正则。validate_file 函数对每条字幕按多个维度顺序检查。问题按严重程度分为严重和警告两级，严重问题返回退出码 1。V3.3 移除 JAPANESE_KANA 正则常量及对应假名残留检测逻辑。

strip_japanese.py 从 _final.srt 中去除日文行，支持多行中文译文。import common 以触发日志初始化。

rename_suffix.py 是通用的重命名工具，通过命令行参数指定后缀。

run_all.py 是入口调度器。在 import common 之前设置 LOG_FILE 环境变量，并加载项目根目录的 .env 文件（V3.4.1 新增）：手动解析 KEY=VALUE 行（跳过注释和空行，去除值引号），通过 os.environ.setdefault 注入，已存在的系统环境变量优先。DEEPSEEK_API_KEY 从硬编码改为 os.environ.get 读取（V3.4.1）；V3.5 起 ZHIPU_API_KEY 移除，改用 TAVILY_API_KEY 和 EXA_API_KEY。get_env 函数构建注入给子脚本的环境变量字典（含所有配置参数）。run_step 函数用 subprocess.run 调用单个脚本并检查返回码。main 函数打印横幅、收集激活的步骤（六个布尔开关）、按序执行、最后打印完成信息。非交互式终端检测避免在计划任务中阻塞。

---

## 依赖关系

Python 层面的依赖（V3.4 起共六个包，V3.0 移除了未使用的 zhipuai）。faster-whisper 是 OpenAI Whisper 的 CTranslate2 重写版，支持 int8 量化推理，是本地 GPU 转写的核心，亦支持 initial_prompt 和 hotwords 参数引导识别。openai 库仅用于调用 DeepSeek API（通过 base_url 指向 api.deepseek.com）。V3.5 起联网搜索改为直连 Tavily（api.tavily.com/search）与 Exa（api.exa.ai/search）的 REST API，使用标准库 urllib.request，不引入第三方依赖，也不再经 LLM 中转压缩。V3.4 新增四个音频预处理依赖：librosa（音频读取与 16kHz 重采样）、noisereduce（稳态噪声抑制）、scipy（Butterworth 高通滤波）、soundfile（PCM_16 wav 输出），均在 preprocess_audio 函数中惰性导入，未开启预处理时无额外开销。requirements.txt 锁定了依赖：faster-whisper>=1.2.1、openai>=1.0.0、librosa>=0.10.0、noisereduce>=3.0.0、scipy>=1.11.0、soundfile>=0.12.0。

外部运行环境需要 FFmpeg（Whisper 读取任意格式音频的依赖，需要可执行文件在系统 PATH 中）和 CUDA Runtime（GPU 推理的依赖，通常随 NVIDIA 驱动安装，RTX 40 系列需要驱动版本 536.40 以上）。如果遇到 cublas64_12.dll 缺失的错误，可以通过 pip 安装 nvidia-cublas-cu12 和 nvidia-cudnn-cu12 补充运行时库，无需安装完整的 CUDA Toolkit。

模型文件在首次运行时自动从 Hugging Face Hub 下载，缓存在用户的 .cache 目录下。large-v3 约 3GB，large-v3-turbo 约 1.5GB。start.bat 等启动脚本已内置国内镜像（HF_ENDPOINT=https://hf-mirror.com）；命令行运行时若网络受限，可自行设置环境变量 HF_ENDPOINT 指向镜像站，或者在别的机器上下载后手动复制到缓存目录。

---

## 版本历史

以下为各版本的主要架构变化记录，按时间倒序排列。正文各章节描述的是当前版本（V3.5.0）的架构。

### V3.5.0

联网搜索提供商替换。common.py 的智谱搜索（zhipu_search，经 glm-4-flash 的 web_search 工具中转并压缩结果）整体移除，替换为直连 Tavily 与 Exa 的 REST API（标准库 urllib.request，不引入第三方依赖）。新增 _http_post_json（HTTP POST JSON 工具）、_tavily_search（POST api.tavily.com/search，Authorization: Bearer 认证，search_depth=basic）、_exa_search（POST api.exa.ai/search，x-api-key 认证，contents.text 限长 400 字符）、web_search 统一入口（替换 zhipu_search：已配置的提供商全部查询、按 URL 去重合并、总长截断 3500 字符、查询缓存与"错误结果不缓存"逻辑沿用）以及 get_search_keys（替换 get_zhipu_key，返回 tavily/exa 双 key）。统计口径从 zhipu_calls 改为 tavily_calls/exa_calls 分列，费用估算改为 SEARCH_COST_PER_CALL 常量（默认 $0.005/次，可调）。run_all.py 的 ZHIPU_API_KEY 替换为 TAVILY_API_KEY/EXA_API_KEY（.env 加载与 get_env 传递同步更新），validate_final.py 的 API artifact 模式表相应更新。DeepSeek 侧的 web_search 工具协议（SEARCH_TOOL）与多轮搜索流程完全不变——仅执行端换了提供商。另修复十余项问题：空内容重试违反 tool_calls 协议（400 错误）、低置信度标记（低信頼度/認識不良）误判为 critical 导致流水线中断、translate 空译文整批丢弃、GPU 转写混入并行融合阶段（新增阶段2.5 主线程串行补跑）、device 硬编码（新增 _detect_device 自动回退 CPU/float16）、requirements 收紧 faster-whisper>=1.2.1、rename_suffix 静默覆盖（改先备份）、match_scripts 拆分失败状态残留与非交互终端 input 阻塞、review_japanese 全篇审校批大小钳制、parse_srt_full int() 容错、run_all 绝对路径、音频重名检测、estimated_tokens 1:1 保守估算、API_ARTIFACT_PATTERNS 去重与 review_japanese batch_count 死代码清理。后续又有环境适配类修复：_detect_device 的 GPU 检测从 torch 改为 ctranslate2（项目不依赖 torch，无 torch 的 GPU 机器此前会被误判为 CPU 回退）；老 GPU 加载 int8_float16 失败时自动降级 float16 兜底；新增 sitecustomize.py 幽灵 Ctrl+C 防御（经 PYTHONPATH 在启动早期生效，SIGINT 防抖：第 1 次忽略、第 2 次才中断）；start.bat 等启动脚本重写（内置 HF 国内镜像 hf-mirror.com、PYTHONPATH 设置、venv 缺失时回退备用路径）；run_all.py 在 import common 前打印依赖加载提示。

### V3.4.1

API Key 安全加固。run_all.py 中的 DEEPSEEK_API_KEY 和 ZHIPU_API_KEY 从硬编码字符串改为 os.environ.get 读取，彻底消除源码泄露风险。run_all.py 在 import common 之前新增约 8 行 .env 文件加载逻辑：读取项目根目录的 .env 文件，逐行解析 KEY=VALUE 对（跳过注释行和空行，去除值的引号），通过 os.environ.setdefault 注入——已存在的系统环境变量优先，.env 文件仅作补充。手动解析而非引入 python-dotenv，避免新增依赖。项目根目录新增 .env 文件存真实 Key（.gitignore 已排除）和 .env.example 模板（提交到仓库供其他用户参照）。移除 run_all.py 的 git skip-worktree 标记（V3.4 之前用于保护工作区真实 Key 不被提交，现已无硬编码敏感信息，标记不再需要）。common.py 的 get_deepseek_client 和智谱搜索函数从环境变量读取 Key 的逻辑不变，只是 Key 的来源从 run_all.py 硬编码改为 .env 文件或系统环境变量。

### V3.4

Whisper 转写准确率改进。ensemble_transcribe.py 新增 preprocess_audio 函数——librosa 以 16kHz mono 读取音频，scipy.signal 的 Butterworth 4 阶高通滤波（80Hz 截止）去除低频隆隆声，noisereduce 稳态噪声抑制（stationary=True，prop_decrease=0.7 保守强度避免误伤耳语），RMS 归一化到 -20dBFS 并以峰值限幅 0.95 避免硬削波，soundfile 输出为 PCM_16 wav；所有依赖采用惰性导入，仅在 ENABLE_AUDIO_PREPROCESS 开启时加载，未启用时无额外内存占用。transcribe_with_model 在转写前调用预处理，结果缓存为 output/<base>_preprocessed.wav 支持断点续跑（存在则跳过）。VAD 参数针对 ASMR 耳语场景调优：threshold 从 0.3 降至 0.2（更敏感，捕获低音量耳语）、min_speech_duration_ms 从 100 降至 50（不丢弃短促气声词）、speech_pad_ms 从 600 增至 800（前后多留缓冲避免削头去尾）。新增 initial_prompt 引导（默认"ASMR作品、囁き、耳かき、癒し系、日本語、優しい声"，可覆盖为作品专有术语）和 hotwords 热词（逗号分隔，默认空）。所有新增参数通过环境变量从 run_all.py 注入，子脚本环境变量不存在时使用内置默认值。requirements.txt 追加 librosa>=0.10.0、noisereduce>=3.0.0、scipy>=1.11.0、soundfile>=0.12.0 四个依赖。注：noisereduce 的频谱门限对稳态噪声（底噪、雨声）效果好，对背景音乐等非稳态干扰改善有限，后者需要人声分离（Demucs）。

### V3.3

台本模式效率优化与流程精简。ensemble_transcribe.py 在台本模式下跳过 Turbo 转写（仅跑 V3），融合阶段采用简化的 V3+台本 prompt（不再对比 V3 与 Turbo）；mismatch 处理从 sys.exit(1) 改为自动回退——补跑 Turbo 后用原 V3+Turbo prompt 重新融合，mismatch 文件名记录到 output/script_mismatch.json。review_japanese.py 启动时检测 script_mismatch.json，若存在则仅审校列表中的文件。run_all.py 的 STEP2 在台本模式下默认跳过，仅当 STEP1 产出非空 mismatch 列表时触发。translate.py 输入逻辑改为以 *_ensemble.srt 为基础扫描，对每个文件优先使用 *_reviewed.srt，不存在则回退到 *_ensemble.srt，使 STEP2 跳过时 STEP3 仍可正常运行；混合场景（部分文件经审校、部分未审校）亦兼容。match_scripts.py 新增 LLM 文件名匹配阶段（正则失败后由 LLM 判定 match/split/none）与合并台本检测（单一台本被 ≥2 个音频模糊命中时自动转为 pending_split）；extract_track_id 替代 extract_track_number 支持 1A/1B 等数字+字母组合；幂等检查扩展为四类失效条件（台本修改、音频列表变化、音频修改、台本列表变化）。validate_final.py 移除假名残留检查（JAPANESE_KANA 正则及对应检测逻辑）。无台本流程完全不变。

### V3.2

新增台本辅助功能。match_scripts.py 作为 STEP0 在 ensemble 之前运行，负责台本的自动匹配（同名加模糊）、合并台本拆分（正则优先 LLM 兜底）、人工验证交互。ensemble_transcribe.py 在融合阶段将台本文本作为权威参考注入 user_input，扩展 FUSION_TOOL 增加 error 字段以支持台本不匹配时的报错终止（V3.3 起改为自动回退）。common.py 的 FUSION_TOOL schema 相应扩展（subtitles 与 error 二选一，required 改为空数组）。新增 SPLIT_TOOL 工具定义在 match_scripts.py 内部（仅此脚本使用）。默认关闭（ENABLE_SCRIPT=False），不影响现有流程。

### V3.1

ensemble_transcribe.py 重构为两阶段分离架构——阶段1串行转写所有文件（GPU 独占），阶段2用 ThreadPoolExecutor 并行调用 DeepSeek 融合所有文件（网络 I/O 并发），消除原"每文件转写+融合串行"模式下融合阶段的串行等待；原 process_one_audio 函数拆分为 transcribe_one_audio 和 fuse_one_audio 两个独立函数；_fusion_parser 从闭包提升为模块级函数；阶段2前预初始化 DeepSeek client 消除多线程首次调用 race；去掉文件间的 time.sleep(5) 固定等待。

### V3.0

全阶段并行处理（跨文件加跨批次），DeepSeek API 调用阶段耗时大幅缩短；所有可调参数集中到 run_all.py 顶部配置区，通过环境变量传递给子脚本；新增文件日志功能（通过 monkey-patch builtins.print 实现）；融合阶段改用 submit_fusion 工具进行 Function Calling 结构化输出；翻译和审校提示词优化；终审上下文推断能力泛化；搜索相关提示词根据开关动态拼接；后处理脚本 strip_japanese.py 集成为可选步骤；rename 脚本合并为通用工具。

---

## AI 参与说明

本项目通过人机协作方式完成。所有 Python 脚本和文档初稿均由 DeepSeek-V4-Pro（V2.0 及以前）和 GLM-5.2（V3.0 至 V3.4.1）根据人类作者的设计要求生成。人类作者负责：定义项目目标和应用场景、选定技术路线和架构方案、确定所有关键参数、测试和验证输出质量、以及做出发布和许可证决策。整个开发过程采用迭代对话模式：人类作者提出需求或反馈问题，AI 提供分析、建议或修改后的代码，人类作者测试并验证，若有新问题则继续反馈。所有技术决策均由人类作者最终确认，AI 编写的所有代码和文档均经过人类作者的审查和测试。

---

## 许可证

本项目采用 GNU General Public License v3.0 or later (GPL-3.0-or-later) 协议发布。你可以自由地使用、修改和分发本软件，但必须保留原作者署名并以相同协议发布衍生作品。

Copyright (c) 2025-2026 Kochiya3309
